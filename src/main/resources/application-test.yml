spring:
  artemis:
    mode: embedded
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/song_pdb_test
    username: camel_test
    password: camel_test_password
    driver-class-name: oracle.jdbc.driver.OracleDriver
  flyway:
    reinitialize-schema: true
    placeholders:
      tablespace: "PUBLIC_PERM_TBS"